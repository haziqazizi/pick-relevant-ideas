# stable-paths

Scripts need predictable input and output paths.

Use for generated artifacts, reports, logs, screenshots, state files, caches,
and daemon metadata.

## Actions

1. Document default paths.
2. Allow explicit path overrides.
3. Avoid writing into surprising directories.
4. Create parent directories intentionally.
5. Print every created or changed path.

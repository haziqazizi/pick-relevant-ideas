# copy-paste-examples

Every command path should have a realistic example.

Use for scripts that future agents or humans must discover.

## Actions

1. Add at least one successful example to `--help`.
2. Add one dry-run or check-only example for write commands.
3. Use real-looking paths and flags.
4. Keep examples current when flags change.
5. Prefer examples that can run in a fresh checkout.

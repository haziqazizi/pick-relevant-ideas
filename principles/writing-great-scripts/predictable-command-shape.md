# predictable-command-shape

Use consistent command grammar.

Use for multi-command CLIs.

## Actions

1. Choose a pattern such as `tool resource verb` or `tool verb resource`.
2. Keep flag names consistent.
3. Use the same output conventions across commands.
4. Avoid aliases that hide different behavior.
5. Keep destructive verbs visually obvious.
